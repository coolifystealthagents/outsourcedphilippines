import { Header, Footer } from '../components';import { blogPosts, site } from '../data';export default function Blog(){return <><Header/><main className='section'><div className='container'><p className='eyebrow'>{site.brand}</p><h1>Philippines staffing guides</h1><p className='lead'>Plan the role, choose the first tasks, check a provider, and prepare the first week with Filipino talent.</p><div className='cards'>{blogPosts.map(p=><a className='card' href={`/blog/${p.slug}`} key={p.slug}><h3>{p.title}</h3><p>{p.excerpt}</p><span className='pill'>{p.minutes} min read</span></a>)}</div></div></main><Footer/></>}
