import {notFound} from 'next/navigation';import {Header,Footer} from '../../components';import {researchPosts} from '../../fleet-content';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)notFound();return <><Header/><main className="fleet-main"><article className="section article-shell"><p className="eyebrow">Philippines staffing research · {post.published}</p><h1>{post.title}</h1><p className="lead">{post.excerpt}</p><h2>Key Stats</h2><p>{post.statistic}</p><h2>Methodology</h2><p>{post.methodology}</p><h2>Key Takeaways</h2>{post.body.map((x,i)=><p key={i}>{x}</p>)}<h2>Sources</h2><ol>{post.sources.map(source=><li key={source}><a href={source} rel="noreferrer">{source}</a></li>)}</ol><h2>Related Research</h2><ul>{post.related.map(slug=><li key={slug}><a href={`/research/${slug}`}>{slug}</a></li>)}</ul></article></main><Footer/></>}
